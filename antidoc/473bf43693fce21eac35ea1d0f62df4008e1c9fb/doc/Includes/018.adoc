== Custom - Timeout.lvclass

*Responsibility:*
No description found (add content in lvclass description)

*Version:* 1.0.0.2

[plantuml, format="svg", align="center"]
....
@startuml
set namespaceSeparator :
skinparam ClassBackgroundColor #FFFFFF
skinparam ClassBorderColor #555555
skinparam ClassFontColor #000000
skinparam ClassHeaderBackgroundColor #808080

skinparam top to bottom direction
class "Pop-Up.lvlib:Custom - Timeout" {
{field} -Timeout (s) : Double Float
{static} {method} +Display Timeout Popup (Custom)()
{method} #Display()

}
"Pop-Up.lvlib:PNG Parent" <|-- "Pop-Up.lvlib:Custom - Timeout"



@enduml
....

=== Methods

.Functions (non private scope only)
[cols="<.<4d,<.<8a,<.<12d,<.<1a,<.<1a,<.<1a", %autowidth, frame=all, grid=all, stripes=none]
|===
|Name |Connector pane |Description |S. |R. |I.

|Display Timeout Popup (Custom)
|image:Pop_Up.lvlib_Custom___Timeout.lvclass_Display_Timeout_Popup_(Custom).vi.png[Pop-Up.lvlib:Custom - Timeout.lvclass:Display Timeout Popup (Custom).vi]
|The timeout popup will display information to the user and wait the specified timeout depending on the number of seconds passed into this VI.

**Note:** This popup type allows the user to define a custom image for the display. The developer should test all settings prior to deployment to make sure that all images and text fit on the screen correctly.

**Note:** The function **Close Pop-Up.vi** can be used to close this dialog early.

Inputs: +
-Graphic [path] - path to the image file to display +
-Message [String] - String Variable to populate Display with +
-Theme [lvclass] - the theme for the style of the popup +
-Message Info [typedef enum] +
    -Message Height [I32] - the height for the message text box (-1 = match height of image) +
    -Message Width [I32] - the width for the message text box (-1 = match height of image) +
    -Message Location [Ring] - the location of the image relative to the message text box (left,
      right,bottom, top) +
-Timeout (s) [DBL] - the amount of time to show the popup +

**Code Developed By:** +
Daniel Coons +
https://github.com/danielcoons/tsc-pop-up
|image:empty.png[empty.png]
|image:empty.png[empty.png]
|image:empty.png[empty.png]

|Display
|image:Pop_Up.lvlib_Custom___Timeout.lvclass_Display.vi.png[Pop-Up.lvlib:Custom - Timeout.lvclass:Display.vi]
|This vi displays the custom timeout Popup.

**Code Developed By:** +
Daniel Coons +
https://github.com/danielcoons/tsc-pop-up
|image:scope-protected.png[scope-protected.png]
|image:empty.png[empty.png]
|image:empty.png[empty.png]
|===

**S**cope: image:scope-protected.png[] -> Protected | image:scope-community.png[] -> Community

**R**eentrancy: image:reentrancy-preallocated.png[] -> Preallocated reentrancy | image:reentrancy-shared.png[] -> Shared reentrancy

**I**nlining: image:inlined.png[] -> Inlined

=== Class Constant VIs

[NOTE]
====
No Constant VIs Found
====
