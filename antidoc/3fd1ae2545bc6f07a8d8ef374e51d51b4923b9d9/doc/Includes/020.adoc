== PNG Parent.lvclass

*Responsibility:*
No description found (add content in lvclass description)

*Version:* 1.0.0.0

[plantuml, format="svg", align="center"]
....
@startuml
set namespaceSeparator :
skinparam ClassBackgroundColor #FFFFFF
skinparam ClassBorderColor #555555
skinparam ClassFontColor #000000
skinparam ClassHeaderBackgroundColor #808080

skinparam top to bottom direction
class "Pop-Up.lvlib:PNG Parent" {
{static} {method} -PNG Picture Setup()
{static} {method} -PNG Picture Size Reader()
{method} #Display()
{method} #Template Pattern - Caller()

}
"Pop-Up.lvlib:Popup" <|-- "Pop-Up.lvlib:PNG Parent"



@enduml
....

=== Methods

.Functions (non private scope only)
[cols="<.<4d,<.<8a,<.<12d,<.<1a,<.<1a,<.<1a", %autowidth, frame=all, grid=all, stripes=none]
|===
|Name |Connector pane |Description |S. |R. |I.

|Display
|image:Pop_Up.lvlib_PNG_Parent.lvclass_Display.vi.png[Pop-Up.lvlib:PNG Parent.lvclass:Display.vi]
|This is the override method required for each child of the pop up class.  It is the pop up that will display to the user.

To extend this function, create a child class of the pop up class. Then create an override method for this VI. **Display.vi** is a protected function, therefore it cannot be called in a main application. This VI must be called by a caller function. See one of the standard pop-ups for a setup example.

**Code Developed By:** +
Daniel Coons +
https://github.com/danielcoons/tsc-pop-up
|image:scope-protected.png[scope-protected.png]
|image:empty.png[empty.png]
|image:empty.png[empty.png]

|Template Pattern - Caller
|image:Pop_Up.lvlib_PNG_Parent.lvclass_Template_Pattern___Caller.vi.png[Pop-Up.lvlib:PNG Parent.lvclass:Template Pattern - Caller.vi]
|The TEMPLATE PATTERN OOP VI will define the way that the Display function will be called in the PNG type pop-up class. 

**Code Developed By:** +
Daniel Coons +
https://github.com/danielcoons/tsc-pop-up
|image:scope-protected.png[scope-protected.png]
|image:empty.png[empty.png]
|image:empty.png[empty.png]
|===

**S**cope: image:scope-protected.png[] -> Protected | image:scope-community.png[] -> Community

**R**eentrancy: image:reentrancy-preallocated.png[] -> Preallocated reentrancy | image:reentrancy-shared.png[] -> Shared reentrancy

**I**nlining: image:inlined.png[] -> Inlined

=== Class Constant VIs

[NOTE]
====
No Constant VIs Found
====
